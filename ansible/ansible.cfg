# Run from the ansible/ directory, or with ANSIBLE_CONFIG pointing here.
[defaults]
inventory = inventory/hosts.yml
roles_path = roles
interpreter_python = /usr/bin/python3
display_skipped_hosts = false
