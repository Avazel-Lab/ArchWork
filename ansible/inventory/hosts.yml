---
# Groups express the profile, host names identify the box (D-010).
#
# A second laptop joins the laptop group without any variable file changing.
# Nothing here or in the roles reads the host name to decide behaviour.
all:
  children:
    desktop:
      hosts:
        hmlxdesktop01:
    laptop:
      hosts:
        hmlxlaptop01:
