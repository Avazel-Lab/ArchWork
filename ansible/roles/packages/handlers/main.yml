---
- name: Regenerate the initramfs
  # D-027. modconf in HOOKS (scripts/archwork-install.sh) copies
  # /etc/modprobe.d into the initramfs, so the nouveau blacklist only takes
  # effect there once this runs.
  ansible.builtin.command: mkinitcpio -P
  changed_when: true
  become: true
