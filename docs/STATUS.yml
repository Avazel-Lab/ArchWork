# Current state only. No history: git holds that.
# Milestone definitions live in docs/plan.md. Decisions live in docs/decisions/.
# Validated by scripts/check-plan-status.py.
---
schema: 1
updated: 2026-08-29

phase: M3

milestones:
  M0:
    status: complete
    evidence:
      commit: 4a387fa4e2b7d1dc4f5f3b77ef4b456684b3516d
      date: 2026-08-27
  M1:
    status: complete
    evidence:
      commit: f4bd326fa3311d5efa93522ab2dab2e0a92a3b1e
      date: 2026-08-27
  M2:
    status: complete
    evidence:
      commit: d16cf4c6cdd04a6814e7e04c26844bf1515bd139
      date: 2026-08-28
  M3:
    status: in-progress
  M4:
    status: not-started
  M5:
    status: not-started
  M6:
    status: not-started
  M7:
    status: not-started
  M8:
    status: not-started
  M9:
    status: not-started
  M10:
    status: not-started

# A rebuild claim without a commit SHA is rejected by the checker.
# Clean install from the ISO, M1 assertions, the D-016 reconciliation twice
# with the second run reporting zero changed tasks, and the recovery UKI.
# Desktop profile on this commit. Both profiles passed on d16cf4c, at M2.
#
# Everything M3 asks of a machine, on a session greetd starts through
# start-hyprland with its configuration in Lua (D-026): login at the real
# greeter, all 21 session assertions including the eight for Tailscale, the
# desktop driven by its own keybindings through lock and unlock, and a portal
# file picker from PDF Arranger and from Okular. The seven M2 service state
# assertions passed with it, and the second reconciliation reported changed=0.
#
# The theming criterion has evidence for the first time rather than a verdict.
# The captures show the portal file chooser dark and matching the desktop,
# where every earlier run had it light against it. D-021 leaves the verdict to
# a person looking at those captures, and that has not happened yet.
#
# Tailscale is asserted only as far as a logged out machine allows. Split DNS
# against a real tailnet waits on the D-006 auth key.
last_rebuild:
  status: passed
  commit: 0d20ddfa9b3592ef3740f6f25b402afdb9984537
  date: 2026-08-29

next_actions:
  - Look at the captures from the next run and judge the theming criterion,
    the last M3 criterion a machine cannot assert for itself. Materia and
    Papirus are chosen and applied, so the portal chooser should now be dark
    rather than light against the desktop
  - Run the laptop profile against the M3 criteria. Every run so far has been
    the desktop profile, and the session set is shared between them, so it
    should hold and has never been shown
  - Answer the plan gap in D-024. No milestone installs the bulk of
    applications.md, and M7 could pass today with "full desktop" meaning a
    compositor and a terminal

blockers: []
